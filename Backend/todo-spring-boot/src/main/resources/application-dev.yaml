#docker-compose.yaml파일 사용할때.
spring:
  datasource:
    url: jdbc:mysql://localhost:3306/todo?serverTimezone=Asia/Seoul&characterEncoding=UTF-8
    username: todo_user
    password: todo_pass
    driver-class-name: com.mysql.cj.jdbc.Driver

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true